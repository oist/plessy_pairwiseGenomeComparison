{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/pairalign/master/assets/schema_input.json",
    "title": "nf-core/pairalign pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "fasta_1": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.f(ast)?a(\\.gz)?$",
                "errorMessage": "FastA file for genome must be provided, cannot contain spaces and must have extension '.fa.gz' or '.fasta.gz'"
            },
            "fasta_2": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.f(ast)?a(\\.gz)?$",
                "errorMessage": "FastA file 2 cannot contain spaces and must have extension '.fa.gz' or '.fasta.gz'.  We are going to ignore this file."
            }
        },
        "required": ["sample", "fasta_1"]
    }
}
